# INKURIMENT の開発ルール

`https://towerinkuri.vercel.app/` ／ `https://github.com/sakuraraichi-lang/TOWERINKURI`

---

## 常時やること（ユーザーの指示・2026-09-20）

> 「このバランス調整や開発は常に続けて欲しいんだけど、
> 適宜Vercelにも反映して、パッチノートとして残していって欲しい」

### 1. 手を止めない

バランスと開発は**継続の作業**。区切りごとに止まらず、
「本当に確認を取らないとどうにもならないこと」以外は続ける。
承認待ちのものがあっても、**承認の要らない作業まで一緒に止めない。**

### 2. 出したら必ず本番へ

`main` に push すると Vercel が自動で配信する。**コミットしたら push まで必ずやる。**

配信されたか確かめる方法：

```bash
curl -s https://towerinkuri.vercel.app/src/balance.js | grep -m1 "const BUILD"
```

HTML は `no-store` にしてある（`vercel.json`）。
`X-Vercel-Cache: HIT` が出て古い版が返るときは、エッジのキャッシュを疑う。
以前これで「更新されていない」と見えたことがある（age 746秒）。

### 3. パッチノートを残す

**版を上げたら、必ず `docs/PATCHNOTES.md` に書く。** 書き方の決まりは同ファイルの冒頭。

- 新しいものが上
- **プレイヤーから見て何が変わったか**を先に。実装の話は後ろ
- 数字は**実測値だけ**。「たぶん速くなった」は書かない
- 直した不具合は**どう壊れていたか**も書く（同じ壊れ方を繰り返さないため）

ゲーム内では、ホーム右上の版をタップするとパッチノートへの入口が出る
（`PATCHNOTES_URL` in `src/balance.js`）。

### 4. 版を上げる

`src/balance.js` の `BUILD`。日付4桁＋英字（`0920v` → `0920w`）。
**挙動が変わったら上げる。** ドキュメントだけの変更では上げない。

---

## 確定した方針（2026-09-21 ユーザー決定）

### バランスは「大きく」取る。プレイヤーを絞る方向で考えない

> 「敵側の難易度調整カーブが正しいから、味方を絞るって見方をやめよう、
> **大は小を兼ねる**し、**ストップポイントと跳ね上げポイントを意図的に作りやすくなる**、
> 疑うべきは、**君のデフレ思考**だ、**もっと大味にやったっていい**」

**禁じ手にする考え方**

- 「プレイヤーが強すぎるから、収入／効果量／上限を絞る」
- 「敵の曲線は設計どおりだから、プレイヤー側を合わせる」
- 刻みの小さい調整（1.25 → 1.30 のような）でお茶を濁す

**採る考え方**

- **足りないのは敵側。敵の指数を増やす。** プレイヤーの指数は減らさない
- **大きく取る。** 余裕を先に作っておけば、あとからカードや系統を足しても壊れない。
  絞ってあると、足すたびに絞り直しになる
- **上限（cap）は「壊れるから置く」ものであって、「バランスを取るため」に置かない。**
  `waveCountMax` が 260 で切っていたせいで、プレイヤーが敵誘引を買っても
  敵が増えず、追いかけっこが止まっていた（実測 2026-09-21）
- **ストップポイント（詰まる章）と跳ね上げポイント（急に楽になる章）は、
  意図して置くもの。** 幅が大きいほど置きやすい
- 刻みは大きく振ってから狭める。小さく振って「変わりませんでした」を繰り返さない

---

### 敵のインフレに躊躇しない。その分こちらもインフレさせる

> 「そうだね、必要、**その分こっちの武器もインフレさせればいいから敵のインフレには躊躇しないで**」

`chapterMul` や `waveCountGrowth` を上げるのを、上限や副作用を心配して小さく刻まない。
**敵を上げる → こちらも上げる**、を繰り返してよい。

### タワーのHPを鍛えるスキルツリーは置かない

> 「変えない、だから**基本タワーのHP鍛えるスキルツリーはノーサンキュー**、
> せめてカードで固定値上昇」

- **漏れの重さは章によらず1体＝ライフ1のまま**（変えない）
- **ライフを伸ばすツリーのノードは置かない。** いまある `core`（防衛線・ライフ+3/段・上限なし）は**撤去対象**
- ライフを増やすなら**カードの固定値上昇まで**（`gen_armor`「増設装甲」ライフ+6 のような形）

理由：ライフのツリーは「漏れてもいい数」を無限に買えるので、
**漏れの重さを据え置く限り、難易度そのものを買って消せてしまう。**

### スキルツリーは「取り切り」にして、深くする

> 「スキルツリーが浅すぎる、**3枠しかない**、もっと広げて、
> インクリメンタルでこんなに浅いスキルツリー見た事ない」

> 「スキルツリーの浅い原因、**同じスキルのレベルをずっと強化し続けるから開放していく感じがない**、
> **取り切りで次のツリーに進んで、コスト曲線で目指すところをつけよう**や、
> そしたら**インフレも制御しやすい**し、**スキル一個取っただけで激変！が味わえる**し」

**いまの形（禁じ手にする）**
- 1ノードを `max: Infinity` で延々レベルアップ（実測で Lv62 まで積まれていた）
- 効果が1段 ×1.13 のような薄い刻みなので、1回押しても何も起きた気がしない
- 総倍率が「いくらでも」なので、**インフレの天井が設計で決まらない**

**これから採る形**
- **1ノードは取り切り（`max: 1`）。** 取ったら次のノードが開く
- **1ノードの効果は大きく。**「1個取っただけで激変」が目標
- **間隔はコスト曲線で作る。** 収入は実測 ×4.8/章 なので、
  **1段あける章数 ＝ log(costG) ÷ log(4.8)**。5章あけたいなら costG ≈ 2,548
- **枝ぜんぶ取ったときの総倍率が、設計上の天井そのものになる。**
  ここが決まるから、敵側をどこまで上げればよいかも決まる

### 設置枠は「値段の跳ね上がり」で間隔を作る（進行では固く縛らない）

> 「例えばお金で買えるにしてもそれ相応にコスト重くしたり、
> カードで永続強化されるにしても、**配られる枚数に気使え**」
> 「無尽蔵に増やしまくれるか章毎に絞るかじゃなくて、**スキルツリーの深さとかで対応**しなよ、
> **1基増やしたら次増やすコストめちゃくちゃ跳ね上がって、実質5章後ぐらいにしか取れない**とかさ」

- **`maxPerClear` のような固い進行縛りは使わない。** コスト曲線で間隔を作る
- **「設置枠はお金で買えない」という以前の原則は、この指示で置き換わった。**
  買えてよい。ただし**次の1基が実質5章後になる値段**にする
- **カードや遺物で配る場合も、配布枚数と上限に気を配る**（上限なしの `flat` を作らない）

---

### 目標プレイ時間は「3時間前後」

> 「3時間前後のゲームとして作りましょう、ただし、ここで確実に難易度カーブとデザイン、
> マップ生成ロジックや、未着手部分を埋めて行きます、
> **ただしこれからの開発で3時間前後から増えていく分には問題ない**としてくださいね」

- **もとの目標帯 5〜6時間は取り下げ。** 実測（2026-09-21）が 2.5〜3.4時間だったため
- **3時間を下限として扱う。** 今後の開発で伸びるぶんには止めない
- これを基準に**難易度カーブ・デザイン・マップ生成・未着手部分**を埋めていく
- `docs/DESIGN-MASTER-2026-09-20.md` の §11（想定プレイ時間）、§3-1 の見積列、
  §2-2〜§2-6 の各章の所要、§6-4 の経済目標帯は、**この3時間基準で引き直す**

---

## 必ずやる大きなタスク（2026-09-21 にユーザーから）

### マップのグリッドを脱却する

> 「マップがコピペすぎるのがよくないね。デザインはいずれ全て整えていくにしろ、
> **マップの形状とかで難易度も調節していく予定**だから、**早めにグリッド式のマップは脱却したい**。
> あと、敵の出方も、一マスからってよりこの入り口（数マス分、もちろん**グリッド廃止後は壁に開いた穴**）
> から**ゾロゾロと出てくる感じ**がいい。
> そもそもマップバリエーションに関してはいつか手を出さなきゃいけないところだから、
> **必要なタスクとして記憶しておいて**」

**いま何が問題か**

- 30章ぶんのマップは `tools/gen30.py` が「型（幕ごとに固定）＋乱数」で作っているが、
  **同じ型の章は見た目がほぼ同じ**。コピペに見える。
  `tools/gen30.py` の検査出力がそのまま証拠になっている（2026-09-21 実測）：

  | 章 | 型 | 通路 | 地面 | 経路長 |
  |---|---|---|---|---|
  | 15〜20 | `trident` | **全章 94** | **全章 206** | 22,23,24 前後 |
  | 21〜27 | `hall` | 92〜104 | 186〜198 | 21〜23 |

  **6章ぶん・7章ぶんが、通路の数まで同じ。** 乱数が効いているのは端の数タイルだけ
- 15×21 のタイル格子に固定されていて、通路が必ず直線と45度になる
- **出現口が1タイル**。敵が1マスから1体ずつ出てくる

**どこへ向かうか**

| | 今 | 目指す形 |
|---|---|---|
| 地形 | 15×21 のタイル格子 | **格子に依らない形**（多角形・曲線の通路） |
| 難易度 | 数値だけで調整 | **マップの形でも調整する** |
| 出現口 | 1タイル | **壁に開いた穴（数マス分の幅）** |
| 敵の出方 | 1体ずつ点から | **穴からゾロゾロ出てくる** |

**先に決めないといけないこと（未着手）**

- 経路探索を格子BFSから何に変えるか（ナビメッシュ／経路をポリラインで持つ など）
- 「置ける場所」をどう表すか（今は `#` タイル）
- ~~敵の押し合い（`Crowd`）が格子前提なので、そこも変わる~~
  **【訂正 2026-09-21】これは私の見立て違いだった。** `Crowd` が使う格子は
  `BAL.crowdCell`（24px）の**画素空間の一様ハッシュ**で、マップのタイル（`TILE=40`）とは無関係。
  `Grid`（70px）も同じ。**タイルに縛られているのは「壁に押し出さない」判定の1行だけ**
  （`src/combat.js:67`。同じ形の判定が `combat.js:737` と `state.js:554` にもあり計3箇所）。
  押し合いそのものは書き直さなくてよい
- 既存の30章をどう移行するか

**いつやるか**：時期は未決。ただし**「いつか」ではなく必要なタスク**として扱う。
デザインを整える前に着手したい、とのこと。

#### 進み具合（2026-09-22・ver 0922ac 時点）

| | 状態 |
|---|---|
| 出現口が「壁に開いた穴」 | **済**（`holeW` 2〜4タイル） |
| 穴からゾロゾロ出る | **済** |
| 絵が格子に見えない | **済**（六角＝`MapGen.HEX_R` 30 で通路も壁も焼く） |
| マップの形で難易度を付ける | **済**（`BAL.mapShape` ／ 盤の大きさ ／ 口の数） |
| 経路探索・押し合い・設置 | **タイルのまま。**（六角から焼き直すので下の層は何も変わらない） |

**第19〜30章は「ブロック式」で作る。**（`MapGen.makeBlock` ／ `BAL.blockFromDepth`）
折れ線（`MapGen.make`）は壁を彫って道を作るので、**彫り残しが広場として残る。**
ブロック式は逆に、置ける塊を撒いて隙間ぜんぶを道にするので、道から遠い地面ができない。
実測：通路まで2マス以内の地面が 95〜100%（折れ線は 47〜78%）。

**ブロック式で触ってよいノブ**（`BAL`）

| | |
|---|---|
| `blockFromDepth` | どの深さから使うか。**0.62 未満にしない**（19×27 以下では経路の下限20を構造的に割る） |
| `blockGap` | 塊どうしの隙間＝**道の太さ**。密度を上げたいならここ |
| `blockFingerMin/Max` | 指の長さ |
| `blockChamber` | コアの周りの空き |
| `blockRoadMin/Max` | 通路の総量の帯（**315 × 割合**。折れ線用の既定値では全部落ちる） |

**やって駄目だったこと（繰り返さない）**

- 撒く回数を増やす ── 塊の数は変わらない。**面積で先に埋まりきる**
- 指を同心の帯に並べて半ピッチずらす ── 斜めの指を軸沿いの長方形で近似する都合で
  当たり判定が膨らみ、ほとんど置けない
- 盤を広げて経路を伸ばす ── 足りない。**同心の囲いを、入り口を層ごとにずらして重ねる**

---

### エフェクトの要望は溜めてある（2026-09-22 ユーザーから）

> 「今はそっちのバランス調整してるだろうからさ、**後でデザインに手をつける時に思い出して欲しい**」

**9件の一覧が `docs/DESIGN-EFFECTS-2026-09-22.md` にある。**
血飛沫（敵の色）・コインが弾ける・キラキラ集まる・加減速マス・炎上・毒・氷・
壁弾着の破片、そして**「絵に合う中身（スリップダメージ）が無ければ付ける」。**

**デザインに手をつけるときは、必ずこのファイルを開く。**
ユーザーが名指しした一覧なので勝手に減らさない。
「類似のものを足してよい」とも言われている。

---

### 後半・ボス・ラスボスのマップ（2026-09-22 ユーザー指示）

> 「ボス章や後半ステージ、ラスボスは**広いマップ＋複雑な地形＋分断された敵の襲撃**を
> 意識して作ってみて、こうすればもしかしたら20基でもクリア出来ないってなるかもしれないし、
> **そしたら基数上限を増やしてもいい。**
> ただ、もちろんだけど、**設置できる数的に対策出来ないだけで火力だけが過剰、
> みたいな状態は健全じゃない**ことは留意して欲しい」

> 「広大なマップで自分が真ん中にいて、**6方向くらいから分断された敵に襲われる**から
> 武器の置き方を考える必要があるとかになると面白いよね、
> **細い道は足止めが効きやすい、太い道は火力を集中させないとどんどん出てくる**とかさ」

**採る形**
- 中央にコア、N方向から**別々の道**（`MapGen` の `spokes`）
- 道ごとに太さを変える（`laneW`）。細い道＝足止め、太い道＝火力
- **弾が壁を抜けなくなった（0922q）ので、別の方角は別の砲でしか守れない**
- 20基で足りなければ**上限を増やしてよい**

**健全さの判定は測って出す。**
> **枠だけ増やしたら楽勝になるなら、それは「配置だけを問う偽の難易度」。**
同じマップを「20基」と「40基」で測り、40基で漏れがほぼ0になるなら、
そのマップは火力を問うていない。**両方が要る形になっているかを必ず確かめる。**

**却下された案（同じ日に試して駄目だった）**
- **道の長さで難易度カーブを引く** … ユーザー
  「それってタワーディフェンスの醍醐味ないよね、ただただ火力足切りされてる感じ」。
  数字としては効く（6.9〜9.5倍）が、置き方の工夫が生まれないので**採らない**
- **盤を左右／上下で2分割する** … 道が一直線になって短くなるだけだった

---

## このプロジェクトの決まり

### 測ってから言う

**「クリアできた」をバランスの根拠にしない。** 測定器は `tools/sim.html`。

**【2026-09-21 追加・3回やらかした】ユーザーに数字を出す前に、
その数字が何本の測定から出たものかを必ず書く。1本なら「1本」と書く。**

このセッションで、1回の観測を代表値として報告する失敗を**3回**やった。

| 報告した内容 | 実際 |
|---|---|
| 「第6章以降は待ち率100%＝ワンパン」 | 1本だった。3本で測ると 69〜74% |
| 「火力が1,300倍過剰」 | 算出が誤り。digest の dps は死んだ敵への超過分を含む |
| 「転生しても再攻略の時間が変わらない」 | 測定器がスキップボタンを押していなかった |
| 「転生4回で13章」（0921q） | 測定器が**スナイパーを第1章から持っていた**。本体は第2章の突破報酬 |
| 「第10章で武器12種を測った」（0921s/t/u） | **測定器が黙って第1章に差し替えていた。**`Game.startPrep` は開いていない章を `ch1` にする |
| 「刀が最強・触手が最弱」（0921s） | **見る指標が違った。**dps は強さを表さない（下記） |
| 「三枚重ねは積むほど弱くなる」（0922e の作業中） | **4本では足りなかった。**12本で測ると逆に良くなる |

**3つ目・4つ目が示すとおり、「何本測ったか」だけでは足りない。
測定器がプレイヤーと同じ操作・同じ持ち物になっているかも疑うこと。**

**本体を変えたら、`tools/sim.html` の対応箇所も同じコミットで直す。**
いまずれやすいのは次の3組。**片方だけ直すと、測定だけが別のゲームになる。**

| 本体 | 測定器 |
|---|---|
| `cards.js` の `STARTER_CARDS` | `WEAPON_START` |
| `tools/gen30.py` の `CH_WEAPON` | `WEAPON_AT`（**`ch > N`**。報酬は突破時にもらうので使えるのは次の章から） |
| `Skill.buyOrder` / `Combat.spawnSecFor` | 同じものを呼ぶ（写さない） |
| 章の解放（`stageUnlocked`） | `unlockTo(stageId)`。**開けずに頼むと黙って第1章になる** |
| コインと恒久の土台 | `opts.coins` / `opts.deep`。**深い章は、まっさらなセーブでは設計どおり手も足も出ない** |

### 武器とカードは「漏らした数」で測る。与ダメージでは測れない

**与ダメージ（dps）は強さを表さない。** 実測（第10章・単独・4シード）：

| | dps 1位 | dps 最下位級 |
|---|---|---|
| 刀 | **1.00** | 単独突破 **0/4** |
| 凍結装置 | 0.08 | 単独突破 **4/4** |

さらに悪いことに、**効率が上がると与ダメージは下がる。**
テスラの「連鎖増幅」は与ダメージで測ると 0.75倍（＝弱くなったように見える）が、
漏らした数で測ると 233 → 136 で**明確に良くなっている。**
連鎖が広がるぶん**過剰ダメージが減る**ため。

**武器の仕事は漏れを止めること。**そのまま漏らした数で測る。
測定器の `opts.lives` でライフを厚くし、**必ず5ウェーブ最後まで回す**
（途中で落ちると「そこまでの漏れ」しか数えられず、弱いものほど数字が小さく出る）。

### 武器を比べるなら第10章では飽和する。第15章で測る

**第10章は12種のうち半分が漏れ0になる。**差が出ないので「横並び」に見えてしまう。
2026-09-22 に第10章で触手を調整して「掴む3で手裏剣と並ぶ」と結論したが、
第15章で測り直すと**中央値231で下から2番目**だった。

**武器を比べる標準の設定（2026-09-22 以降）**

```
runStage('ch15', [武器], 60, { coins: 1e9, deep: {prestiges:2, deepest:17}, lives: 400 })
```

12シードの**漏れの中央値**を見る。この設定なら 0〜71 の帯に12種が並ぶ。

**同じ設定・同じシードでも、測るたびに振れる。**
テスラの同一設定を2回測って 75 と 104 だった。**6本では ±30 は雑音。**
採用する値は必ず12本で確かめ直す。

### 弱い武器に「威力」を足しても、たいてい何も起きない

2026-09-22、下位3種すべてで同じことが起きた（6シード・中央値）。

| | ダメージ×3 |
|---|---|
| スナイパー | 439 → **442** |
| テスラコイル | 339 → **403** |
| 触手 | 424 → **424** |

**当たった敵には、もともと過剰に入っている。**効くのは
**「1発が何体に届くか」**（貫通・弾の太さ・連鎖数・掴める数・レート）だけ。
弱い武器を見たら、まず**威力ではなく到達体数**を疑う。

### 測定器では見えないバグがある。**キャッシュは測定器が勝手に捨てている**

2026-09-22、総点検で見つけた例。

**転生した直後の出撃に、恒久の土台（Legacy）が乗っていなかった。**
`Legacy.of` は `prestiges > 0` で初めて効くのに、`Relic.mods` は
転生前に作ったキャッシュ（土台1倍）を返し続けていた。

**測定では一度も出なかった。** 測定器は `opts.deep` を渡し、
`runStageInner` がその中で `Relic.invalidate()` を呼ぶため、
**毎回たまたまキャッシュが捨てられていた。**

→ **キャッシュを持つものは、測定器の外（本体の導線）でも確かめる。**
   いま持っているのは `Relic._cache`（遺物）と `Stage._cache`（地形）。
   前者は deepest・prestiges・遺物の枚数が変わったら捨てる。

### 条件を比べるときは、交互に回す。別々に測った数字を並べない

**同じ設定・同じ12シードを測り直すと、中央値が 78 / 91 / 106 と振れる**（2026-09-22 実測）。
**別の回で取った数字どうしを比べると、2倍程度の差は作れてしまう。**

この日、それで2回まちがえた。

| 見えた差 | 交互に回したら |
|---|---|
| 通路の量 28 対 60（「効く」と判断しかけた） | 34/31/48 対 28/48/33 ＝ **順序が入れ替わる。差は無い** |
| 経路長 192 対 95（2.1倍と報告した） | 178/194/190 対 26/28/20 ＝ **6.9〜9.5倍**（もっと大きかった） |

**やり方：条件A→条件B→条件A→条件B… と交互に回して、同じ回の中で比べる。**
3回そろって順序が同じなら本物。1回でも入れ替わったら、まだ何も言えない。

### 漏れは二値で振れる。カード級の効果を見るには12本以上

漏れは「持ちこたえる（ほぼ0）」か「決壊する（数百）」のどちらかに寄る。
**4本だと結論の符号が変わる。**「三枚重ね」は4本で 23→70（罠に見えた）、
12本で 109→74（むしろ良い）。武器どうしの大きな差は4本で見えるが、
カード1枚ぶんの差を見るなら12本以上。

- 数字を出すときは、何回測った平均かを書く
- 1回の測定は振れる。同じ設定でも 21〜29分の幅が出たことがある
- **敵の供給数で頭打ちになる指標に注意。**「撃破数」は天井に当たるので、
  強さを測るときは「漏れをどれだけ止めたか」を使う

### ビルドしない

`<script>` を並べるだけの構成。**ES modules は `file://` で動かないので使わない。**
音も画像も持たず、その場で生成する（`src/audio.js` は WebAudio、武器の絵は Canvas）。

### セーブは3層

| | 消えるタイミング | 中身 |
|---|---|---|
| `run` | 出撃ごと | 敵・弾・そのランのカード |
| `meta` | **転生で消える** | コイン・スキルツリー |
| `perm` | **消えない** | カード・パック・遺物・到達記録・換装の持ち物 |

`SAVE_KEY = 'inkuriment_save_v3'`

### 設計の原則（実測で決まったもの）

- **設置枠はお金で買えない。** 踏破したステージ数でしか増えない。
  火力・カバー範囲・漏れにくさが同時に増え、他の全強化と掛け算になるため
- **武器に「どこを狙うか」を決めさせない。** 撃ちっぱなしの首振り。
  指定攻撃は、プレイヤーが置いた円に降らせる
- **パックの解放は転生回数。** ステージ進行は転生で戻るので、そこに紐づけると2周目で意味が消える
- **恒久強化（遺物）は初回転生から。** それ以前に触らせない
- **累乗で伸びるものに上限を置く。** 上限なしの累乗は必ず壊れる
  （集金効率が ×1.11/段・上限なしで Lv39・×153倍まで伸びていた）

### 主な資料

| | |
|---|---|
| `docs/PATCHNOTES.md` | **版ごとの変更。ここを最初に更新する** |
| `docs/DESIGN-MASTER-2026-09-20.md` | 製品版の全体設計（30章・解放順・成長曲線・実装計画） |
| `docs/DESIGN-ECONOMY-2026-09-20.md` | 経済とデッキの設計 |
| `docs/DESIGN-AUTOMATION-2026-09-20.md` | 自動化と進行の設計 |
| `docs/REPORT-*.md` | 回ごとの作業報告 |

---

## エージェント

`.claude/agents/` にいる。**手を止めるときは `gal-audit` の判定を受ける**
（`.claude/hooks/gal_stop.py` が止める）。判定には**残作業を
「承認が要る／要らない」で分けた一覧**を必ず渡す。
エージェントの発言は要約せず、そのまま会話に残す。
