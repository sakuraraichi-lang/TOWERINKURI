# INKURIMENT の開発ルール

`https://towerinkuri.vercel.app/` ／ `https://github.com/sakuraraichi-lang/TOWERINKURI`

---

## 常時やること（ユーザーの指示・2026-09-20）

> 「このバランス調整や開発は常に続けて欲しいんだけど、
> 適宜Vercelにも反映して、パッチノートとして残していって欲しい」

### 1. 手を止めない

バランスと開発は**継続の作業**。区切りごとに止まらず、
「本当に確認を取らないとどうにもならないこと」以外は続ける。
承認待ちのものがあっても、**承認の要らない作業まで一緒に止めない。**

### 2. 出したら必ず本番へ

`main` に push すると Vercel が自動で配信する。**コミットしたら push まで必ずやる。**

配信されたか確かめる方法：

```bash
curl -s https://towerinkuri.vercel.app/src/balance.js | grep -m1 "const BUILD"
```

HTML は `no-store` にしてある（`vercel.json`）。
`X-Vercel-Cache: HIT` が出て古い版が返るときは、エッジのキャッシュを疑う。
以前これで「更新されていない」と見えたことがある（age 746秒）。

### 3. パッチノートを残す

**版を上げたら、必ず `docs/PATCHNOTES.md` に書く。** 書き方の決まりは同ファイルの冒頭。

- 新しいものが上
- **プレイヤーから見て何が変わったか**を先に。実装の話は後ろ
- 数字は**実測値だけ**。「たぶん速くなった」は書かない
- 直した不具合は**どう壊れていたか**も書く（同じ壊れ方を繰り返さないため）

ゲーム内では、ホーム右上の版をタップするとパッチノートへの入口が出る
（`PATCHNOTES_URL` in `src/balance.js`）。

### 4. 版を上げる

`src/balance.js` の `BUILD`。日付4桁＋英字（`0920v` → `0920w`）。
**挙動が変わったら上げる。** ドキュメントだけの変更では上げない。

---

## 確定した方針（2026-09-21 ユーザー決定）

### バランスは「大きく」取る。プレイヤーを絞る方向で考えない

> 「敵側の難易度調整カーブが正しいから、味方を絞るって見方をやめよう、
> **大は小を兼ねる**し、**ストップポイントと跳ね上げポイントを意図的に作りやすくなる**、
> 疑うべきは、**君のデフレ思考**だ、**もっと大味にやったっていい**」

**禁じ手にする考え方**

- 「プレイヤーが強すぎるから、収入／効果量／上限を絞る」
- 「敵の曲線は設計どおりだから、プレイヤー側を合わせる」
- 刻みの小さい調整（1.25 → 1.30 のような）でお茶を濁す

**採る考え方**

- **足りないのは敵側。敵の指数を増やす。** プレイヤーの指数は減らさない
- **大きく取る。** 余裕を先に作っておけば、あとからカードや系統を足しても壊れない。
  絞ってあると、足すたびに絞り直しになる
- **上限（cap）は「壊れるから置く」ものであって、「バランスを取るため」に置かない。**
  `waveCountMax` が 260 で切っていたせいで、プレイヤーが敵誘引を買っても
  敵が増えず、追いかけっこが止まっていた（実測 2026-09-21）
- **ストップポイント（詰まる章）と跳ね上げポイント（急に楽になる章）は、
  意図して置くもの。** 幅が大きいほど置きやすい
- 刻みは大きく振ってから狭める。小さく振って「変わりませんでした」を繰り返さない

---

### テストプレイと導線の方針（2026-09-26 ユーザー決定）

> 「初めての人の体験を図るのは3章までにしましょう、チュートリアルもそこで打ち止めです、
>   このゲームは基本的な戦略TWDなので突拍子のない武器の置き方などにはあまりゲーム内で対応しません」
> 「テストプレイを回すのは上手な置き方に絞ってください、公式解方さえこちらが知っているのであれば問題ないです」
> 「少し導線を設置するだけでかなり良くなります」「短い一言でいいです」

- **初めての人の測定とチュートリアルは第3章まで。**それより先で、変わった置き方に合わせてゲーム側を直さない
- **テストプレイ（通し・章ごとの測定）は上手な置き方（測定器の PLACE_MODE 'range'）だけで回す。**初心者の置き方の測定は第1〜3章に限る
- 初めて入る画面・初めて使う武器には**短い一言の案内**（導線）を置く。長い説明はしない
- ライフの数字は**⚙でオン・オフ**（2026-09-26）。カード3択の上の枠は、**武器のアイコンと、その武器に取ったカードの数**を並べ、触ると中身が見える形（ユーザーの理想）

### チュートリアルで「敵の通るガイドを撃てるように置く」を教える（2026-09-26 ユーザー）

> 「敵が通るガイドをみて、そこを打つようにしておくと良いよ！みたいなチュートリアルを少し挟んであげると、親切なゲームになりそうですね」

- きっかけ：オタクくんのテストプレイ（測定器・第1章・12本ずつ）。**コアの周りに固めて向きは置いたままだと 0/12 突破・撃破0〜5**、
  通路に沿って並べると 0/12（ウェーブ4〜5）、射線と向きを選ぶと 12/12。**置き方を知らないと第1章で全滅する**
- 準備フェーズのチュートリアルに「点線（敵の通り道）が扇に入るように置く」を入れる。戦闘中に準備の指示を出し続けない

### 敵のインフレに躊躇しない。その分こちらもインフレさせる

> 「そうだね、必要、**その分こっちの武器もインフレさせればいいから敵のインフレには躊躇しないで**」

`chapterMul` や `waveCountGrowth` を上げるのを、上限や副作用を心配して小さく刻まない。
**敵を上げる → こちらも上げる**、を繰り返してよい。

### 盤は全員同じ30章に固定する。二度と変えない（2026-09-26 ユーザー決定）

> 「盤が変わる仕様を撤廃しましょう、全て固定にしないと戦略性が産まれません、
>   明らかに難しすぎるマップや、詰み要素のあるマップ、簡単すぎる、問題があるものは報告します」
> （選択肢で「全員同じ30章」を選択）

- 盤は `BAL.mapSeed`（3333）だけで決まる。`perm.mapSeed` / `perm.mapRoll` は読まない（項目は互換のために残してある）
- **負けが続いたときの作り直し・転生で未到達の章を作り直す、の両方を撤去した。**「詰んだら引き直せる」で逃がさない
- 詰む盤・難しすぎる盤は、**種を変えずに盤の作り方か、その章の盤そのものを直す**（種を変えると全員の30章が変わる）
- 測定器の通しは全部同じ盤で走る。**盤の検査で別の種を試すときだけ** `BAL.mapSeed` を差し替える（`BAL_PRISTINE` も一緒に）

### 敵誘引はスキルツリーから外す。ハードモードの難易度調整として残す（2026-09-26 ユーザー決定）

> 「原因が誘引でした…時間に対しての圧縮、漏れ出る敵が原因ですね、つまり実質的な難易度上昇カーブが誘引では激しいです。
>   誘引なしで遊ぶとちょうど良く感じられます」
> 「誘引そのものがゲームのハードモードに相当する難易度調整バーとなっています、ゲームからコメントアウトせず、これを後で活かせる要素として使うと良さそう」
> 「あまりゲームの経済カーブには問題なく（それでも1〜2章転生ループはどうにかした方がいいが）…スキルツリーから一旦消してしまっても良い」

- `BAL.lureInTree` false でツリーに出さない。**買ってあっても効かせない**。定義と `mods.spawn` は残す
- 誘引は敵のHPを変えない。**湧く時間が14秒固定（`spawnRateMax` 999）なので、増えた数がそのまま密度になる**のが「数以上に難しい」の正体
- プレイヤーの感想「10章に時間がかかる」「完璧は卓選び前から強くないと」は、誘引を取った遊び方から出ていた。
  **感想で難易度の話が出たら、まずその人が誘引（や難易度を上げる選択）をしていたかを確かめる**
- 経済カーブは大筋で問題なし（ユーザー）。1〜2章の転生ループは 0926a で塞いだ

### タワーのHPを鍛えるスキルツリーは置かない

> 「変えない、だから**基本タワーのHP鍛えるスキルツリーはノーサンキュー**、
> せめてカードで固定値上昇」

- **漏れの重さは章によらず1体＝ライフ1のまま**（変えない）
- **ライフを伸ばすツリーのノードは置かない。** `core`（防衛線・ライフ+3/段・上限なし）は**撤去済み**
- ライフを増やすなら**カードの固定値上昇まで**（`gen_armor`「増設装甲」ライフ+6 のような形）

理由：ライフのツリーは「漏れてもいい数」を無限に買えるので、
**漏れの重さを据え置く限り、難易度そのものを買って消せてしまう。**

### スキルツリーは「取り切り」にして、深くする

> 「スキルツリーが浅すぎる、**3枠しかない**、もっと広げて、
> インクリメンタルでこんなに浅いスキルツリー見た事ない」

> 「スキルツリーの浅い原因、**同じスキルのレベルをずっと強化し続けるから開放していく感じがない**、
> **取り切りで次のツリーに進んで、コスト曲線で目指すところをつけよう**や、
> そしたら**インフレも制御しやすい**し、**スキル一個取っただけで激変！が味わえる**し」

**いまの形（禁じ手にする）**
- 1ノードを `max: Infinity` で延々レベルアップ（実測で Lv62 まで積まれていた）
- 効果が1段 ×1.13 のような薄い刻みなので、1回押しても何も起きた気がしない
- 総倍率が「いくらでも」なので、**インフレの天井が設計で決まらない**

**これから採る形**
- **1ノードは取り切り（`max: 1`）。** 取ったら次のノードが開く
- **1ノードの効果は大きく。**「1個取っただけで激変」が目標
- **間隔はコスト曲線で作る。** 収入は実測 ×4.8/章 なので、
  **1段あける章数 ＝ log(costG) ÷ log(4.8)**。5章あけたいなら costG ≈ 2,548
- **枝ぜんぶ取ったときの総倍率が、設計上の天井そのものになる。**
  ここが決まるから、敵側をどこまで上げればよいかも決まる

### 設置枠は「値段の跳ね上がり」で間隔を作る（進行では固く縛らない）

> 「例えばお金で買えるにしてもそれ相応にコスト重くしたり、
> カードで永続強化されるにしても、**配られる枚数に気使え**」
> 「無尽蔵に増やしまくれるか章毎に絞るかじゃなくて、**スキルツリーの深さとかで対応**しなよ、
> **1基増やしたら次増やすコストめちゃくちゃ跳ね上がって、実質5章後ぐらいにしか取れない**とかさ」

- **`maxPerClear` のような固い進行縛りは使わない。** コスト曲線で間隔を作る
- **「設置枠はお金で買えない」という以前の原則は、この指示で置き換わった。**
  買えてよい。ただし**値段の跳ね上がりで間隔を作る**（「実質5章後」はユーザーの例え。実装はスキルツリーの `units` の連なり14段・1段+2基）
- **カードや遺物で配る場合も、配布枚数と上限に気を配る**（上限なしの `flat` を作らない）

---

### 目標プレイ時間は「3時間前後」

> 「3時間前後のゲームとして作りましょう、ただし、ここで確実に難易度カーブとデザイン、
> マップ生成ロジックや、未着手部分を埋めて行きます、
> **ただしこれからの開発で3時間前後から増えていく分には問題ない**としてくださいね」

- **もとの目標帯 5〜6時間は取り下げ。** 実測（2026-09-21）が 2.5〜3.4時間だったため
- **3時間を下限として扱う。** 今後の開発で伸びるぶんには止めない
- これを基準に**難易度カーブ・デザイン・マップ生成・未着手部分**を埋めていく
- `docs/DESIGN-MASTER-2026-09-20.md` の §11（想定プレイ時間）、§3-1 の見積列、
  §2-2〜§2-6 の各章の所要、§6-4 の経済目標帯は、**この3時間基準で引き直す**

**【2026-09-25 実測】測定器の「分」と、何で伸びるか**
- 測定器の分（`progressAsync` の `mins`）は**戦闘の時間だけ・1倍速**。メニュー・パック開封・配置の時間は入らず、
  2〜4倍速も考えていない。**実際の遊ぶ時間とは別物**なので、3時間の判定にそのまま使わない
- 0925e 時点で 30章まで 115〜134分（4本）
- **後半の敵HP（`lateEnemyMul.g`）を上げても、分はほとんど伸びない。**周が増える（×1.10→×1.30 で 7〜8周→8〜10周）が、
  Legacy が周ごとに追いつくので詰まった周は短く終わる。**12本・シードごとに交互**で 中央値 117.5分 → 125分（+7%）、
  幅 108〜137 → 107〜166、12本中8本で長く4本で短い。（最初に2本で「伸びない」と書いたのは早すぎた。12本で直した）
- **1ウェーブの湧く長さ（`spawnSeconds` 14→20）は +23%。**12本・シードごとに交互で 中央値 117分 → 143.5分、**12本すべてで長い**
  （幅 108〜138 → 134〜160）。最初に2本で「+6〜17%」と書いたのは過小だった。**時間を伸ばす手として効く**が、1戦ごとのテンポが変わる
- 3時間（測定器の分で180分）に届かせるには、湧く長さをさらに伸ばすか、章あたりのウェーブ数を増やすなどが要る（テンポに直結するのでユーザーに相談）
- **【2026-09-25 ユーザー】「プレイ時間は全く気になりません、倍速のおかげでもあります」**
  ＝ 測定器の分が3時間に届かないことを理由に、湧く長さやウェーブ数を伸ばしに行かない

---

## 必ずやる大きなタスク（2026-09-21 にユーザーから）

### マップのグリッドを脱却する

> 「マップがコピペすぎるのがよくないね。デザインはいずれ全て整えていくにしろ、
> **マップの形状とかで難易度も調節していく予定**だから、**早めにグリッド式のマップは脱却したい**。
> あと、敵の出方も、一マスからってよりこの入り口（数マス分、もちろん**グリッド廃止後は壁に開いた穴**）
> から**ゾロゾロと出てくる感じ**がいい。
> そもそもマップバリエーションに関してはいつか手を出さなきゃいけないところだから、
> **必要なタスクとして記憶しておいて**」

**いま何が問題か**

- 30章ぶんのマップは `tools/gen30.py`（2026-09-24 に削除）が「型（幕ごとに固定）＋乱数」で作っていたが、
  **同じ型の章は見た目がほぼ同じ**。コピペに見える。
  `tools/gen30.py` の検査出力がそのまま証拠になっている（2026-09-21 実測）：

  | 章 | 型 | 通路 | 地面 | 経路長 |
  |---|---|---|---|---|
  | 15〜20 | `trident` | **全章 94** | **全章 206** | 22,23,24 前後 |
  | 21〜27 | `hall` | 92〜104 | 186〜198 | 21〜23 |

  **6章ぶん・7章ぶんが、通路の数まで同じ。** 乱数が効いているのは端の数タイルだけ
- 15×21 のタイル格子に固定されていて、通路が必ず直線と45度になる
- **出現口が1タイル**。敵が1マスから1体ずつ出てくる

**どこへ向かうか**

| | 今 | 目指す形 |
|---|---|---|
| 地形 | 15×21 のタイル格子 | **格子に依らない形**（多角形・曲線の通路） |
| 難易度 | 数値だけで調整 | **マップの形でも調整する** |
| 出現口 | 1タイル | **壁に開いた穴（数マス分の幅）** |
| 敵の出方 | 1体ずつ点から | **穴からゾロゾロ出てくる** |

**先に決めないといけないこと** → **【2026-09-23】3つとも答えが出た**

- ~~経路探索を格子BFSから何に変えるか（ナビメッシュ／経路をポリラインで持つ など）~~
  **変えない。** 通路は折れ線と六角で作るが、**最後にタイルへ焼き直す**ので
  BFS はそのまま使える。絵は六角、判定はタイル。二重に持つほうが安く済んだ
- ~~「置ける場所」をどう表すか（今は `#` タイル）~~
  **六角セルになった（ver 0923a）。** `stage.hexBuildable` / `hexCells` / `hexPick`。
  **武器の大きさ概念は撤去した（ver 0923d・ユーザー指示）。どの武器も六角1つ**
- ~~敵の押し合い（`Crowd`）が格子前提なので、そこも変わる~~
  **【訂正 2026-09-21】これは私の見立て違いだった。** `Crowd` が使う格子は
  `BAL.crowdCell`（24px）の**画素空間の一様ハッシュ**で、マップのタイル（`TILE=40`）とは無関係。
  `Grid`（70px）も同じ。**タイルに縛られているのは「壁に押し出さない」判定の1行だけ**
  （`src/combat.js:67`。同じ形の判定が `combat.js:737` と `state.js:554` にもあり計3箇所）。
  押し合いそのものは書き直さなくてよい
- ~~既存の30章をどう移行するか~~
  **移行済み。第1〜3章も生成できるようになった（ver 0924e）。**
  0922ad の「手書き落ちゼロ」は一度崩れていた（2026-09-24 実測：新規セーブ20種で第1〜3章の25〜40%が
  四角い手書きの盤に落ちる。序盤の検査を折れ線が400枚中1枚しか通せなかった）。
  ユーザーの選択（「序盤専用の生成器を作る」）で `MapGen.makeSerp`（六角の縦の列で往復）を足し、
  **80枚中0枚**になった。**手書きの盤と `tools/gen30.py` / `genmaps.py` は 2026-09-24 に削除した**（ユーザー指示。180枚中0枚が落とし先に回っていた）。
  作れなかったら種をずらして作り直し、それでも駄目なら例外を出す
**いつやるか** → **マップ本体は完了（ver 0922ac〜af）。**
6方向の分断も入った（`holesByDepth` を 3 → 4／6 に上げた・ver 0922ae）。
敵側の倍率（`lateEnemyMul`）と基数の天井（34基）もセットで入っている。

**ただし「方向」と「数値」を分けて読むこと。**

| | 状態 |
|---|---|
| 天井を上げる／敵側を上げる／6方向にする（**方向**） | **ユーザー承認済み** |
| **34基 ／ `lateEnemyMul` g=1.10 ／ `waveCountPerHole` 0.2**（**具体的な値**） | **承認済み。ただし暫定値**（下記） |

**この3つの値は「いまの暫定値」。**（ユーザー 2026-09-23）
> 「維持して良いが、**プレイヤーのフィードバック次第で再度バランス調整が入ることは
>   織り込んでくれ**」

＝ **確定値として扱わない。** 遊んだ感想が来たら動かす前提で、
根拠（何を測ってこの値にしたか）を balance.js のコメントに残してある。
**動かすときは、同じ測り方で取り直して比べること**
（第30章・最前線の条件＝legacyDeep 27 で第30章に挑む・12本の中央値）。

**マップ本体に残りは無い。残っているのは数値の確定だけ。**

#### 進み具合（2026-09-22・ver 0922ac 時点）

| | 状態 |
|---|---|
| 出現口が「壁に開いた穴」 | **済**（`holeW` 2〜4タイル） |
| 穴からゾロゾロ出る | **済** |
| **6方向から同時に襲われる** | **済**（実測 2026-09-23・下記） |
| 絵が格子に見えない | **済**（六角＝`MapGen.HEX_R` 30 で通路も壁も焼く） |
| マップの形で難易度を付ける | **済**（`BAL.mapShape` ／ 盤の大きさ ／ 口の数） |
| 設置 | **六角**（ver 0923a）。武器の大きさ概念は撤去（ver 0923d） |
| 経路探索・押し合い | **タイルのまま。**（六角から焼き直すので下の層は何も変わらない） |

**「6方向から同時に」は成立している。**（実測 2026-09-23・第30章）
`Combat.spawnEnemy` は穴を順に切り替えて流す（1つの穴から `幅×2` 体出してから次へ）ので、
設計書 §14-5 は「複数の穴から同時に出ないのが残課題」と書いていた。
**ウェーブの途中で数えたら、6穴すべてに敵がいた**（約250体が 31〜64 ずつ・ほぼ均等）。
**切り替えが敵の移動時間より十分速いので、出方が順番でも結果は同時になる。**
＝ この残課題は**実装を足さずに解消している。**

**【2026-09-23・ユーザー指示】ブロック式は廃止した。**
> 「全てブロック式は廃止、六角形で固定」

**【2026-09-26】第12〜30章も分岐式（口3・4・6の型）に移した。六角式（`MapGen.makeHex`）は使っていない（`BAL.hexFromDepth` 1.01）。**以下の六角式の記述は経緯として残す。
以前：**第12〜30章は「六角式」で作る。**（2026-09-25 にいったん第8章からにしたが、第8〜11章は分岐式に移した）（`MapGen.makeHex` ／ `BAL.hexFromDepth`）
六角そのものを単位にして、**隣づたいに掘る。**
ブロック式（軸に沿った長方形の塊を撒いて隙間を道にする）は、絵も設置も六角になった以上
**作るときだけ四角い**ので筋が通らなかった。
**`makeBlock` も `BAL.block*` のノブ8個も削除した**（ver 0923d・ユーザー
「ブロック準拠の旧形式はこれから行わないでください、ブロックの名残を消してください」）。
`style:'block'` の分岐も残っていない。

**六角式で触ってよいノブ**（`BAL`）

| | |
|---|---|
| `hexFromDepth` | どの深さから使うか（0 で全章）。**第1〜3章は往復式、第4〜11章は分岐式**（下記） |
| `hexChamber` | コアの周りに開ける半径（六角いくつぶん） |
| `hexRingStep` / `hexDoorN` | 同心の囲いの間隔と入り口の数。**経路を伸ばしているのはこれ** |
| `hexWander` | 道の蛇行 |
| `hexRoadW` | 道の太さ（1＝掘った筋だけ／2＝隣も掘る） |
| `hexRoadMin/Max` | 通路の総量の帯（**315 × 割合**） |

**第1〜3章は往復式（`MapGen.makeSerp`）、第4〜30章は分岐式（`MapGen.makeFork`）。**第4〜11章は口1〜2つの型（2026-09-25）、第12〜30章は口3・4・6つの型（2026-09-26・ユーザー「同様に作りましょう」）。
口3・4・6つの型は「口の並べ方とコアの位置」だけを持ち、各口からコアへひし形（口→分岐→左右の腕→合流→コア）を自動で引く（`diamond`）。
道の組み方も型に含める（'deep' 2回分かれて2回合流／'lad' 腕の途中を横棒でつなぐ）。**口の並べ方だけ違う型は反転すると同じに見える**ので、組み方の違う型を混ぜて、口3つ・4つは7形（7章に1章1形）。
**コアを盤の真ん中に置く型は作れない**（19×27で口からコアまで約10タイル、経路の下限20に届かない）。真ん中のコアは六角式（hexring）に任せる
**湧き口：第1〜7章は1つ、第8〜11章は2つ、第12〜18章は3つ、第19〜25章は4つ、第26章から6つ**（2026-09-25。指定攻撃を湧き口に重ねるだけで済む盤を、ユーザーが許容した第7章までに限るため。折れ線は口を増やせないので、第8〜11章は口2つの分岐式にした）
六角式は小さい盤で経路が伸びない（15×21 で中央値15）ので、序盤は往復で長さを作る。

**往復式で守ること（実測 2026-09-24）**
- **縦の列で往復する。**六角（平らな頭）は縦に並べると辺でぴったり繋がる。横の往復は1六角だと焼くと途切れ、
  2六角だと通路が129タイルで多すぎた
- **列は「中心がタイルの中心から10px以内」の列だけ使う。**境目に来る列（x=360 など）は焼くと1段おきに途切れる
- 列と列の渡りだけ2段の太さ（斜めの一歩で途切れないように）
- **コアは最後の列の奥（60〜90%）。**40%から置くと最後の道が短く、16本中1本が6回とも越えられなかった
- 経路の下限は `BAL.serpRouteMin` 40（30だと第3章が越えにくい盤が出た）
- 比べるときの注意：**手書きの盤は出現口が1タイル、生成した盤は「穴」が2〜4タイル**で敵がまとまって出る。
  コイン固定の単発で手書きと比べると、ウェーブ1の漏れの差（16対29）がライフ24をまたいで結果が割れる。
  序盤は**通し**（`progressAsync`）で章ごとの挑戦回数を見る

#### **盤が活かされているか：敵はレーンに分かれて進む**（2026-09-25・ユーザー指示）

> 「左下が迂回路のようになっているのにも関わらず、右に直通しており、敵が利用しないデッドスペースが多数あります…
>   不要なスペースを除くと1本道の虚無みたいなタワーディフェンスの出来上がりです。
>   私が敵が動いてくる経路、口を増やせ、分断しろと言うのはこう言う事の防止です、
>   マップの広さに対して敵はどう動くか、作ったマップは活かせてるか？これを再度検証し直してください」

**何が起きていたか**：敵はコアからの幅優先探索の「次のタイル」を全員でなぞるだけで、**迂回路は構造上一度も使われなかった。**
通路のうち最短経路のまわり（±1タイル）に入る割合は、第4章から先で **34〜72%（多くは55〜60%）**（種3つ×30章）。

**いまの形**（`stages.js` の `lanes` ／ `MapGen.makeFork` ／ `MapGen.addDetours`）
- **レーン**：口ごとに、最短の道のほかに「前のレーンから離れた道」を探す（前のレーンから**通路を歩いて** `laneSep` 歩以内を重くした最短経路。
  直線距離で見ると、壁1枚向こうの別の通路まで「同じ道」とみなして捨てていた。歩いた距離にして、第12〜30章でレーン1本の口が 約3割 → 約2割弱）。
  長さが最短の `laneMaxRatio` 倍以内で、離れた区間がひと続きで `laneMinSplit` タイル以上あれば採る。最大 `laneMax` 本
- **敵は出るときにその口のレーンへ順番に振り分けられ**、レーンに沿って進む（`Combat.pickLane` ／ `flowTo(c, r, lane)`）。分裂した子は親のレーン
- **第4〜11章は分岐式**（`makeFork`）：口 → 左右の腕（盤の両端を回る）→ 合流、を1〜2回。**分かれ道を作り方で保証する**。
  口2つ（第8〜11章）は上下の縁からそれぞれ中央のコアへ。15×21 だと片側の道が 12〜18 で下限 20 に届かないので、盤を 19×27 にした（`boardByDepth`）。
  **形の種類（型紙・2026-09-25 ver 0925k）**：ユーザー「4〜11章、ほとんどが上下左右を反転させたような…真新しさに欠けます。
  …左右から攻めてくる、上下から攻めてくるなど、バリエーションに富んだデザインで」。
  口1つ＝diamond・offset・ladder・**ring（横の縁から入りコアの周りを両回り）・snake（折り返して箱で分岐）・trident（三叉）**／
  口2つ＝eight・sidecore・**pincer（左右の縁から）・corner（隣り合う2辺から）・parallel（同じ辺から2本）**。
  **同じ周の第4〜7章・第8〜11章で型が重ならないように、種で並べ替えて1章1型を配る**（`Stage.mapRowsFor` → `shape.forkPattern`）。
  **反転だけの違いは「違う形」に数えない。**新しい型は骨格（口の辺・分岐の数・コアの位置）が違うものを足す。
  分岐式で2本取れない口があれば盤ごと作り直す（`Stage._regen`・6回まで）。通路量の上限は分岐式だけ `BAL.forkRoadMax` 165。
  外した型：pinwheel（左上と右下の縁から中央のコア。19×27 では口からコアまで16〜17で下限20に届かない）、diagonal（通る割合 2/15）
- レーンが1本しか取れない口には、壁の中を回り込む**迂回路**を足す（元の道と壁2枚ぶん離す。最短経路が下限か元の85%を割ったら戻す）
- それでも**どのレーンも通らない六角は壁に戻して焼き直す**（`pruneDead`）。六角の単位なので直線で切られない
- 検査：`Stage.usage(st)`（通る割合・口ごとのレーン数）。`Stage.validateAll()` は通る割合が `usageMin`（80%）未満の章を挙げる

**結果**：通る割合は第4〜11章 95〜99%、第12〜30章 79〜97%（多くは90%前後）。種4つで確認。

**やって駄目だったこと（繰り返さない）**
- 出来上がった盤に迂回路を**あとから継ぎ足す**だけで直そうとした ── 近道になって最短経路が付け替わり、元の輪が消えたり、隣の通路とくっついて太い道になるだけだった。
  **形は作り方で保証する**（分岐式）。継ぎ足しは補助にとどめる
- 「別の道」を重なりの割合（6割以下）で判定した ── 同じ太い通路の中を並んで走るだけの道が採れてしまった。**離れた区間の長さ**で判定する

#### **敵が壁をすり抜ける（壁越しの接触）。**（2026-09-25・ver 0925k で直した）

> ユーザー「5章で壁が繋がっていて分離できていなかったりしますね、存在しない経路を通ろうとします、
>   **こういうのも忘れずにチェックしておいてください**」

焼き直しの規則（5点のうち2点）だと、**六角1つの壁を挟んだ2本の通路のタイルが上下左右で接する。**
経路探索はそこを道として通っていた（種1つで30章中28章・146か所）。**生成時の検査も同じ誤りで最短経路を過小評価していた。**
- いまは `MapGen.cover`（タイルごとに焼いた六角）と `MapGen.linker`（同じ六角か隣の六角どうしのタイルだけ1歩で行ける）を、
  経路探索・レーン・押し合い（`st.step`）・`MapGen.check` が共有する
- 隣り合う通路の六角は、中心を結ぶ線のタイルも通路にする（盤の縁で隣どうしが繋がらない穴を、壁越しの接触が埋めていた）
- 行き止まり外し（prune）は、レーンが通る「線だけのタイル」の両端の六角を両方残す（片方だと焼かれず道が切れる）
- **確かめ方**：種を変えて全章で「隣り合う通路タイルで `step` が偽の辺」「レーンの連続2タイルで `step` が偽」（0であること）、
  届かない口、`validateAll` を数える。盤を触ったら毎回これを回す

#### **通路が断片に割れる。**（2026-09-23・4回はまった。原因を2回まちがえた）

**これが六角式でいちばん危ない落とし穴。**

**最初の見立ては外れだった。**「六角は幅60・高さ52でタイルは40なので、
六角1つが覆うタイルが1〜2枚しかなく、焼くと割れる」と結論して、
**タイルを1マスずつ掘って継ぎ足した**。連結は直ったが、
ユーザーに「**ハニカムを無理やり切り貼りした直線通路がある**」と指摘された。そのとおり。

**六角空間で数え直したら、掘った六角そのものが割れていた。**
169個が8つの島（51/42/40/12/7/7/5/5）。
焼き方を多点サンプリングに変えても**島は8個のまま**で、変換の問題ではなかった。
掘り進みが行き止まる（`pick` が見つからず break する）と、口の側とコアの側が別の島になる。

**直し方：六角空間で島どうしを繋ぐ。**
掘り終えたあとに島を数え、コアのいる島へ**六角の隣づたい**に掘って合流させる。
タイルの直線は使わないので、通路の形が最後まで六角のまま。
＝ 未接続が 12枚中4〜7枚 → **0枚**。

#### 盤の縁で「絵と規則」を食い違わせない（2026-09-24・ユーザー指示で撤去）

> 「ハニカム形式なのに無理やり直線にする描画もリファクタリングで削除してください」

以前は焼いたあとに `MapGen.edge()` が外周1マスの通路を壁に戻し、描く側が外周のタイルを
四角く塗り直して食い違いを隠していた。これが**通路をまっすぐ切る線**になっていた。
**両方撤去した。** 六角が覆うところは外周でも通路、盤は四角で切らず輪郭も六角のまま描く。
**描画の側で四角く塗って食い違いを隠す手は、もう使わない。食い違うなら規則の側を絵に合わせる。**

残っている食い違い：焼き直しの規則（「タイルの5点のうち2点以上が通路の六角に入れば通路」）はそのままなので、
**1本の通路の中で、壁の六角の縁を半タイル未満だけ歩ける見た目のずれは残る**（918タイル・0925k より前の測定）。
**別の通路へ抜けるすり抜けは 0925k で別に直した**（`MapGen.cover`／`linker` と `Stage.build` の `step` が、
タイルからタイルへの1歩を「焼いた六角が同じか隣どうし」のときだけ許すようにした。壁越しの接触は起きない）。
残っているのはこの見た目のずれだけで、通路が砕けないための意図的な妥協。
なくすには経路探索そのものを六角にする必要がある。
**教訓：層をまたぐ不具合は、どの層で起きているかを先に数える。**
（六角の成分数とタイルの成分数を両方数えれば、1回で分かった）

**やって駄目だったこと（繰り返さない）**

- ~~ブロック式で撒く回数を増やす~~ ／ ~~指を同心の帯に並べて半ピッチずらす~~
  ── **ブロック式ごと廃止した（ver 0923d）。**もう試す対象が無い
- 盤を広げて経路を伸ばす ── 足りない。**同心の囲いを、入り口を層ごとにずらして重ねる**
- 六角式で掘り方を変えて連結を直そうとする ── 上記。**変換の側を直す**

---

### エフェクトの要望は溜めてある（2026-09-22 ユーザーから）

> 「今はそっちのバランス調整してるだろうからさ、**後でデザインに手をつける時に思い出して欲しい**」

**9件の一覧が `docs/DESIGN-EFFECTS-2026-09-22.md` にある。【2026-09-24】9件とも実装済み**（凍結にスリップを付けないのはユーザー判断）。
血飛沫（敵の色）・コインが弾ける・キラキラ集まる・加減速マス・炎上・毒・氷・
壁弾着の破片、そして**「絵に合う中身（スリップダメージ）が無ければ付ける」。**

**新しいエフェクトの要望が来たら、このファイルの一覧に足してから着手する。**
ユーザーが名指しした一覧なので勝手に減らさない。
「類似のものを足してよい」とも言われている。

---

### 後半・ボス・ラスボスのマップ（2026-09-22 ユーザー指示）

> 「ボス章や後半ステージ、ラスボスは**広いマップ＋複雑な地形＋分断された敵の襲撃**を
> 意識して作ってみて、こうすればもしかしたら20基でもクリア出来ないってなるかもしれないし、
> **そしたら基数上限を増やしてもいい。**
> ただ、もちろんだけど、**設置できる数的に対策出来ないだけで火力だけが過剰、
> みたいな状態は健全じゃない**ことは留意して欲しい」

> 「広大なマップで自分が真ん中にいて、**6方向くらいから分断された敵に襲われる**から
> 武器の置き方を考える必要があるとかになると面白いよね、
> **細い道は足止めが効きやすい、太い道は火力を集中させないとどんどん出てくる**とかさ」

**採った形（ver 0922ae〜af で確定）**
- 中央にコア、**穴6つから別々の道**（`MapGen.makeHex`。六角の隣づたいに掘る）
- **弾が壁を抜けなくなった（0922q）ので、別の方角は別の砲でしか守れない**
- **上限は 20基 → 34基に上げた。**（**ユーザー承認済み・暫定値**。遊んだ感想しだいで動かす）
  ただし**敵側とセット**でしか上げない

**健全さの判定は測って出す。**
> **枠だけ増やしたら楽勝になるなら、それは「配置だけを問う偽の難易度」。**
同じマップを「20基」と「40基」で測り、40基で漏れがほぼ0になるなら、
そのマップは火力を問うていない。**両方が要る形になっているかを必ず確かめる。**

#### 【解決済み 2026-09-23・ver 0922ae〜af】3つ同時に入れた（承認済み・値は暫定）

**それ以前は「偽の難易度」になっていた。**（第30章・ブロック式・26基）

| 振ったもの | 幅 | 漏れの動き |
|---|---|---|
| 火力 | ×0.1 〜 ×100（**1000倍**） | 180 → 172 → 178 → 198 ＝ **動かない** |
| 置ける数 | 12基 → 36基 | 4,737 → 約35 ＝ **135倍** |

漏れていたのは撃ち漏らしではなく「**一度も撃たれていない敵**」で、
砲はどの火力でも射界に入った敵を即死させていた。

**片方ずつ入れても直らなかった。**
穴を3→6にするだけだと 22基479／38基83／58基10 で**基数を増やすだけで解け**、
しかも38基のところで火力を16倍振っても 60/55/55 で動かない。

**ユーザーの承認**
> 「基数の天井を上げて、**ちゃんと適切に火力を上げてようやくクリアできるのであれば**、
>   基数の天井は上げるべきです、ただし最初の頃のように、
>   **雑多に基数を配り雑多に置きまくってクリアは避けて**ください」

**入れた3つ（セットでしか成立しない）**

| | 値 | どこ |
|---|---|---|
| 置ける数の天井 | 20基 → **34基**（14段 × +2） | `skilltree.js` の `units` |
| 後半の敵のHP | 第10章より先、**1章ごとに ×1.10**（第30章で×7） | `BAL.lateEnemyMul` |
| 湧き口 | 3 → **4（19〜25章）／6（26〜30章）** | `BAL.holesByDepth` |
| 口1つあたりの量 | 0 → **0.2**（穴6で敵×2.0） | `BAL.waveCountPerHole` |

**`lateEnemyMul` と `waveCountPerHole` は Legacy の式に入らない。**
＝ プレイヤーの土台を1ミリも削らずに敵だけ上げられる、唯一の入口
（確定方針「プレイヤーの指数は減らさない、足りないのは敵側」）。

**結果（第30章・最前線の条件＝legacyDeep 27 で第30章に挑む・12本の中央値）**

| 火力 | ×0.5 | ×1 | ×2 | ×4 |
|---|---|---|---|---|
| **今** | 7,873 | 6,161 | 4,437 | **2,267** |
| 前 | 180 | 172 | 178 | 198 |

**単調に効く（飽和しない）。** 置ける数だけでは解けない
（34枠あっても火力×0.5 なら漏れ4,384）。

**通し12シード：11本が30章到達、9〜11周、119〜192分（中央値143分）。**
ストップポイントは 15・19・20・23・26・27章（挑戦回数の中央値2）、
第30章だけ毎回6回の上限に当たって次の周へ持ち越す。
**湧き口が増える章（19・26）がそのままストップポイントになっている。**

**倍率を選んだ根拠**

- `lateEnemyMul.g` … 最前線の条件・4シード。
  g=1 は 665/354/267/260（×1より上が効かない）、
  g=1.05 は 2182/957/470/288、**g=1.10 は 3953/2573/1271/549**。
  g=1.18 でも通しは9周127分で完走する（Legacy が周ごとに追いつくので詰まらない）
- `waveCountPerHole` … 通し2シード。0 は口が効かない、**0.2 は9周139〜170分**、
  0.5 は**第25章で4周足踏み**（穴4で×2.5・穴6で×3.5 は重すぎ）
- 天井 … 24段×+1・値上がり×6.5 も試したが**実際に届いたのは22基**で、
  枠が安くなるぶん火力を後回しに買うようになり序盤の前進が
  3→6→10章 から 3→4→7章 に落ちた。**段の間隔は動かさず、1段の効果を倍にした**

**効かなかったノブ（同じことを試さない）**

- **`legacyMargin` 8→2** … Legacy は `enemyHpGrowth^5` で伸びる＝敵HPと式で連動しているので、
  ここで作れる差は高々4倍。1000倍が効かない土俵では意味を持たない
  （6シード×6条件：余裕×8 が 414/410/471、余裕×2 が 354/351/344）
- **Legacy の伸びを緩める** … **禁じ手。** プレイヤーの指数を減らす形で、
  確定方針に真正面から反する。一度案に書いて取り下げた
- **`waveCountMax`（900）** … いまはバランスを持っていない。
  `waveCountGrowth` が 1.01 なので第30章でも 826〜880体／ウェーブで上限に当たらない。
  900 → 9000 に開いても出た敵の総数は 6,300 前後のまま（3シード）

**却下された案（同じ日に試して駄目だった）**
- **道の長さで難易度カーブを引く** … ユーザー
  「それってタワーディフェンスの醍醐味ないよね、ただただ火力足切りされてる感じ」。
  数字としては効く（6.9〜9.5倍）が、置き方の工夫が生まれないので**採らない**
- **盤を左右／上下で2分割する** … 道が一直線になって短くなるだけだった

---

## このプロジェクトの決まり

### 測ってから言う

**「クリアできた」をバランスの根拠にしない。** 測定器は `tools/sim.html`。
**ブラウザなしでも回せる**（2026-09-26・Node v24）：`node tools/simnode.js "<式>"`。sim.html をそのまま読むので測定器は1つのまま。
第1章12本で数字がブラウザと合うことを確かめた（Node 撃破111〜118 ／ ブラウザ 107〜118）。通し1本は約2分なので、シードごとに別プロセスで並べて回せる。
`node` が見つからないときは `C:Program Files
odejs
ode.exe`。
**並べて回すときの注意**：12本を同時に回すと CPU の取り合いで実時間の打ち切り（`REAL_MS` 30秒）が12本中7本に出た。6本ずつ・`REAL_MS = 600000` にすると0本（2026-09-26）。

**【2026-09-21 追加・3回やらかした】ユーザーに数字を出す前に、
その数字が何本の測定から出たものかを必ず書く。1本なら「1本」と書く。**

このセッションで、1回の観測を代表値として報告する失敗を**3回**やった。

| 報告した内容 | 実際 |
|---|---|
| 「第6章以降は待ち率100%＝ワンパン」 | 1本だった。3本で測ると 69〜74% |
| 「火力が1,300倍過剰」 | 算出が誤り。digest の dps は死んだ敵への超過分を含む |
| 「転生しても再攻略の時間が変わらない」 | 測定器がスキップボタンを押していなかった |
| 「転生4回で13章」（0921q） | 測定器が**スナイパーを第1章から持っていた**。本体は第2章の突破報酬 |
| 「第10章で武器12種を測った」（0921s/t/u） | **測定器が黙って第1章に差し替えていた。**`Game.startPrep` は開いていない章を `ch1` にする |
| 「刀が最強・触手が最弱」（0921s） | **見る指標が違った。**dps は強さを表さない（下記） |
| 「三枚重ねは積むほど弱くなる」（0922e の作業中） | **4本では足りなかった。**12本で測ると逆に良くなる |

**3つ目・4つ目が示すとおり、「何本測ったか」だけでは足りない。
測定器がプレイヤーと同じ操作・同じ持ち物になっているかも疑うこと。**

**本体を変えたら、`tools/sim.html` の対応箇所も同じコミットで直す。**
いまずれやすいのは次の3組。**片方だけ直すと、測定だけが別のゲームになる。**

| 本体 | 測定器 |
|---|---|
| `cards.js` の `STARTER_CARDS` | `WEAPON_START`（**本体から作る**） |
| `stages.js` の `STAGES[].reward` | `WEAPON_AT`（**本体から作る**。`ch > N`：報酬は突破時にもらうので使えるのは次の章から） |
| `Skill.buyOrder` / `Combat.spawnSecFor` | 同じものを呼ぶ（写さない） |
| 章の解放（`stageUnlocked`） | `unlockTo(stageId)`。**開けずに頼むと黙って第1章になる** |
| コインと恒久の土台 | `opts.coins` / `opts.deep`。**深い章は、まっさらなセーブでは設計どおり手も足も出ない** |

### 武器とカードは「漏らした数」で測る。与ダメージでは測れない

**与ダメージ（dps）は強さを表さない。** 実測（第10章・単独・4シード）：

| | dps 1位 | dps 最下位級 |
|---|---|---|
| 刀 | **1.00** | 単独突破 **0/4** |
| 凍結装置 | 0.08 | 単独突破 **4/4** |

さらに悪いことに、**効率が上がると与ダメージは下がる。**
テスラの「連鎖増幅」は与ダメージで測ると 0.75倍（＝弱くなったように見える）が、
漏らした数で測ると 233 → 136 で**明確に良くなっている。**
連鎖が広がるぶん**過剰ダメージが減る**ため。

**武器の仕事は漏れを止めること。**そのまま漏らした数で測る。
測定器の `opts.lives` でライフを厚くし、**必ず5ウェーブ最後まで回す**
（途中で落ちると「そこまでの漏れ」しか数えられず、弱いものほど数字が小さく出る）。

### 武器を比べるなら第10章では飽和する。第15章で測る

**第10章は12種のうち半分が漏れ0になる。**差が出ないので「横並び」に見えてしまう。
2026-09-22 に第10章で触手を調整して「掴む3で手裏剣と並ぶ」と結論したが、
第15章で測り直すと**中央値231で下から2番目**だった。

**武器を比べる標準の設定（2026-09-22 以降）**

```
runStage('ch15', [武器], 60, { coins: 1e9, deep: {prestiges:2, deepest:17}, lives: 400 })
```

12シードの**漏れの中央値**を見る。この設定なら 0〜71 の帯に12種が並ぶ。

**【2026-09-24】測定ごとに `Game.newSave()` から始めること。**
`runStage` はスキルツリー・カード・遺物を**前の測定から持ち越す**（毎回コインを渡して60回買い足すので、
並べて回すと後の測定ほどツリーが厚くなる）。同じ武器・同じシードでも、新しいセーブで漏れ424（負け）、
続けて測ると12、と正反対に出た。この日の最初の武器比較の表はこれで汚れていて、
触手と迫撃砲が「強い側」に見えていた（新しいセーブから測ると一番弱い側）。

**同じ設定・同じシードでも、測るたびに振れる。**
テスラの同一設定を2回測って 75 と 104 だった。**6本では ±30 は雑音。**
採用する値は必ず12本で確かめ直す。

### 「当たった敵を速く殺す」軸は、深い章では効かない

**火力・スリップダメージ・状態異常の発生率――どれを振っても漏れが動かない。**
漏れているのは撃ち漏らしではなく「**一度も撃たれていない敵**」だから。

実測（2026-09-23・12本ずつ）：

| 振ったもの | 幅 | 漏れ |
|---|---|---|
| 火力 | ×0.1 〜 ×100（**1000倍**） | 180 → 172 → 178 → 198 |
| 拘束のスリップ | 0 〜 **5**（被弾即死級） | 1,820 → 1,808 → 1,868 |
| 拘束の**発生率**（泡の rate 1.5→6・ヒット3→203） | ＋スリップ 0／0.06／**1.0** | 2,392 → 2,414 → 2,395 |
| **置ける数** | 12基 → 36基 | 4,737 → 約35（**135倍**） |

**効くのは「射界に入るかどうか」だけ。**
新しい軸（スリップ・状態異常・デバフ）を足すときは、
**まず「その軸が漏れを動かせる土俵か」を測る。**
動かないなら、絵として入れるのは良いが、**バランスの手としては数えない。**

### 弱い武器に「威力」を足しても、たいてい何も起きない

2026-09-22、下位3種すべてで同じことが起きた（6シード・中央値）。

| | ダメージ×3 |
|---|---|
| スナイパー | 439 → **442** |
| テスラコイル | 339 → **403** |
| 触手 | 424 → **424** |

**当たった敵には、もともと過剰に入っている。**効くのは
**「1発が何体に届くか」**（貫通・弾の太さ・連鎖数・掴める数・レート）だけ。
弱い武器を見たら、まず**威力ではなく到達体数**を疑う。

### 測定器では見えないバグがある。**キャッシュは測定器が勝手に捨てている**

2026-09-22、総点検で見つけた例。

**転生した直後の出撃に、恒久の土台（Legacy）が乗っていなかった。**
`Legacy.of` は `prestiges > 0` で初めて効くのに、`Relic.mods` は
転生前に作ったキャッシュ（土台1倍）を返し続けていた。

**測定では一度も出なかった。** 測定器は `opts.deep` を渡し、
`runStageInner` がその中で `Relic.invalidate()` を呼ぶため、
**毎回たまたまキャッシュが捨てられていた。**

→ **キャッシュを持つものは、測定器の外（本体の導線）でも確かめる。**
   いま持っているのは `Relic._cache`（遺物）と `Stage._st`（地形。章ごとの記録・2026-09-26 に `_cache` ほか9つの置き場をまとめた）。
   **地形は `Stage.invalidate(章)` でその章だけ捨てられる。**同じ種なら作り直しても同じ盤になる（9章×3回で確認）。
   **盤を黙って差し替えない**（負けが続くと出撃時に引き直していたのが「再挑戦で別の盤」の原因だった。**引き直しそのものを撤去した**・盤は全員同じ30章で固定）
   前者は deepest・prestiges・遺物の枚数が変わったら捨てる。

### 測定器の実時間の打ち切り（`REAL_MS`）が4秒になっていた（2026-09-26 に30秒へ直した）

1回の出撃に許す実時間が4秒で、**後半の周はほぼ毎回ここで打ち切られていた**（通し12本中9本）。打ち切られた出撃はそこで終わるので、
**通しの「何分」が短く、周の数も揺れて出ていた**（種1：4秒で123分・打ち切り2回 → 30秒で151分・0回。実時間は91秒でほぼ変わらない）。
**2026-09-26 より前の通しの分・周の数は、この打ち切りを含んでいる。**第30章に届くかどうかの結論は変わらない。
通しを流すときは `notes` の「打切」を必ず数えて一緒に書くこと。

### 測定器で BAL を差し替えるときは `BAL_PRISTINE` も一緒に（2026-09-26 に踏んだ）

`runStage` は入口で `BAL_PRISTINE` と違う項目を戻す。**転生や周の外で効く値（転生の報酬など）を `BAL` だけ書き換えると、次の `runStage` で黙って戻る。**
`BAL_PRISTINE` は `const` なので `window.BAL_PRISTINE` では見えない（`BAL_PRISTINE[k] = v` と名前で書く）。
これを踏んで、**同じ条件を2回ずつ比べた表を「新しい式が6組とも速い」と読みかけた。**差し替えたら、その値で本当に動いたか（報酬の数など）を必ず1つ確かめる。

### 測定を途中で打ち切ると、`BAL` が差し替わったまま残る

**`withBal` は `finally` で戻すが、実行ごと打ち切られると `finally` は走らない。**
ブラウザから長い測定（12本以上を1回の呼び出しで回す等）を流すと途中で切られることがあり、
**そのあとの測定は黙って別のゲームを測っている。**

2026-09-23 に踏んだ。速度の実測で「×1.5 だけ漏れが半分になる」という非単調な表が出て、
条件を切り分けたら**`enemySpdCap` が 260 のはずが 390、`slotsBase` が 6 のはずが 34** のまま
固定されていた。同じ条件の数字が5倍ずれ、**その回の表は全部捨てた。**
戻してから測り直したら 130→116／180→330／260→813／340→1,299／420→1,708／520→2,311 で、
**きれいに単調だった。**

**対策は入れてある。** `tools/sim.html` が読み込み時に `BAL_PRISTINE` を取り、
`runStage` の入口で `balRestore()` が差し替わった項目を戻して `console.warn` を出す。
**それでも、長い測定は1回の呼び出しに詰め込まず、数本ずつに割って流すこと。**
### 測定器は前の測定の配置を読み戻していた（2026-09-24 に直した）

`Game.startPrep` は保存された配置（`perm.placements`）を読み戻す。測定器の `runStage` は
そのあと自動配置をかけるので、**前の測定の配置と食い合い、同じ条件で置けた数が 7〜33基に割れていた**
（1回目の測定だけ少ない、という出方）。いまは `runStageInner` が始める前に `perm.placements` を空にする。
**2026-09-24 より前の、置ける数が効く測定（天井・基数の表）は、この揺れを含んでいる。**
同じ日に、`runStage` は開いている編成の種類数（`Game.loadoutSlots()`）で武器を切るようにした
（以前は渡した5〜8種を全部並べていた）。
### 同じシードでも再現しない。「1本ずつ比べる」は使えない

`tools/sim.html:288` に既に書いてある（2026-09-23 に独立に踏み直した）。
**600ステップまでは盤面のハッシュが一致するが、40秒あたりからずれる。**
乱数を定数に固定すると完全に再現する（撃破77/77/77）ので、
シミュレーション自体は決定的。ずれるのは乱数の消費本数が枝分かれするため。

**だから「同じシードのA/B」は厳密な対応づけではなく、ばらつきを減らしているだけ。**
本数を増やして中央値で見ること。

**測定器のシード固定が、実プレイに無い詰まりを作る。**
`progressAsync` は周ごとに `seedRng(sd)` で乱数を固定するので、
**同じ章を6回挑戦しても同じ引きが6回続く。**
第1章はそれで 2/16 が6回とも失敗したが、
乱数を固定せずに測ると **24本中0本**（中央値2回・最大4回）だった。
「新規プレイヤーが詰まる」と報告する前に、必ず乱数を固定せずに測り直すこと。

### 条件を比べるときは、交互に回す。別々に測った数字を並べない

**同じ設定・同じ12シードを測り直すと、中央値が 78 / 91 / 106 と振れる**（2026-09-22 実測）。
**別の回で取った数字どうしを比べると、2倍程度の差は作れてしまう。**

この日、それで2回まちがえた。

| 見えた差 | 交互に回したら |
|---|---|
| 通路の量 28 対 60（「効く」と判断しかけた） | 34/31/48 対 28/48/33 ＝ **順序が入れ替わる。差は無い** |
| 経路長 192 対 95（2.1倍と報告した） | 178/194/190 対 26/28/20 ＝ **6.9〜9.5倍**（もっと大きかった） |

**やり方：条件A→条件B→条件A→条件B… と交互に回して、同じ回の中で比べる。**
3回そろって順序が同じなら本物。1回でも入れ替わったら、まだ何も言えない。

### 漏れは二値で振れる。カード級の効果を見るには12本以上

漏れは「持ちこたえる（ほぼ0）」か「決壊する（数百）」のどちらかに寄る。
**4本だと結論の符号が変わる。**「三枚重ね」は4本で 23→70（罠に見えた）、
12本で 109→74（むしろ良い）。武器どうしの大きな差は4本で見えるが、
カード1枚ぶんの差を見るなら12本以上。

- 数字を出すときは、何回測った平均かを書く
- 1回の測定は振れる。同じ設定でも 21〜29分の幅が出たことがある
- **敵の供給数で頭打ちになる指標に注意。**「撃破数」は天井に当たるので、
  強さを測るときは「漏れをどれだけ止めたか」を使う

### ビルドしない

`<script>` を並べるだけの構成。**ES modules は `file://` で動かないので使わない。**
音も画像も持たず、その場で生成する（`src/audio.js` は WebAudio、武器の絵は Canvas）。

### セーブは3層

| | 消えるタイミング | 中身 |
|---|---|---|
| `run` | 出撃ごと | 敵・弾・そのランのカード |
| `meta` | **転生で消える** | コイン・スキルツリー |
| `perm` | **消えない** | カード・パック・遺物・到達記録・換装の持ち物 |

`SAVE_KEY = 'inkuriment_save_v4'`（六角化の前の配置は `perm.hexPlace` で一度だけ捨てる）

### 設計の原則（実測で決まったもの）

- **設置枠は値段の跳ね上がりで間隔を作る。**（2026-09-21 のユーザー指示で「お金で買えない」から置き換わった）
  火力・カバー範囲・漏れにくさが同時に増え、他の全強化と掛け算になるので、
  **遺物の下駄は設置枠に効かせない**（`Skill.lv` の `isUnitNode`）
- **武器に「どこを狙うか」を決めさせない。** 撃ちっぱなしの首振り。
  指定攻撃は、プレイヤーが置いた円に降らせる
- **湧き口にはバリアを置く（`BAL.mouthShieldTiles` 4）。**（2026-09-26・ユーザー「敵の出現位置に対する集中砲火が効きすぎています」）
  穴から通路を4タイル歩くまで敵はダメージも状態異常も受けない。**穴の隣に固めて出た瞬間に倒す置き方**を止めるため。
  測定（第10章・刀と手裏剣12基・12本）：湧き口に固めて口へ向ける置き方は バリアなし110.5 → あり406（全部抜ける）、射線で選ぶ置き方は 0 → 0。
  **測定器の PLACE_MODE 'hug' は「通路に沿って均等に並べる」置き方で、湧き口に固める置き方ではない**（一度取り違えて報告した）。置き方の強さは、遊んだ人のスクリーンショットを先に疑う
- **パック・遺物の上限・自動化の解放は「転生の格」で開ける。**（2026-09-25 までは転生回数だった）
  ステージ進行は転生で戻るので、そこに紐づけると2周目で意味が消える。**かといって回数に紐づけると、浅い転生の繰り返しで買える**
  （プレイヤーの感想「2章転生を繰り返し続けると後ろの章がクリアできる」→ ユーザー「経済カーブが破綻しています」）。
  格は**転生したときの到達章**（legacyDeep・戻らない）から決める（`prestigeRank`・`BAL.rankPerDepth` 4章ごとに1）
- **浅い転生の報酬は減らす。**（今回の突破 ÷ 前回の到達章）^`BAL.prestigeShallowPow`(3)。前回の到達まで進んだ周は満額。
  **固定分をなくす形（0.1×突破^1.5）は序盤を絞るので採らない**（交互6組で遅くなった・パッチノート 0926a）
- **報酬や解放を「回数」に紐づけるときは、1回あたりの手間が最小になる遊び方（第1〜2章だけで転生、など）で稼げないかを先に確かめる**
- **恒久強化（遺物）は初回転生から。** それ以前に触らせない
- **累乗で伸びるものに上限を置く。** 上限なしの累乗は必ず壊れる
  （集金効率が ×1.11/段・上限なしで Lv39・×153倍まで伸びていた）

### 主な資料

| | |
|---|---|
| `docs/PATCHNOTES.md` | **版ごとの変更。ここを最初に更新する** |
| `docs/DESIGN-MASTER-2026-09-20.md` | 製品版の全体設計（30章・解放順・成長曲線・実装計画） |
| `docs/DESIGN-ECONOMY-2026-09-20.md` | 経済とデッキの設計 |
| `docs/DESIGN-AUTOMATION-2026-09-20.md` | 自動化と進行の設計 |
| `docs/archive/` | 09-19〜09-20 の古い計画・作業報告（`REPORT-*` / `PLAN-*` / `DESIGN-V2` など）。いまの設計とは食い違うところがあるので、根拠にしない |

---

## エージェント

`.claude/agents/` にいる。**手を止めるときは `gal-audit` の判定を受ける**
（`.claude/hooks/gal_stop.py` が止める）。判定には**残作業を
「承認が要る／要らない」で分けた一覧**を必ず渡す。
エージェントの発言は要約せず、そのまま会話に残す。
