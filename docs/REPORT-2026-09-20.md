# INKURIMENT 作業報告（第5回）— 取り込み①〜④ 完了

ご指示の実装順 ①〜④ をすべて終えました。**⑤（実機プレイでの判断）をお願いします。**

**遊べます** → `https://towerinkuri.vercel.app/`（既存ゲーム。ステージ一覧に「実験場」「実験場・広大」が増えています）
試作は `https://towerinkuri.vercel.app/proto`

---

## 1. 本体へ取り込んだ機能 / 未取り込みの機能

### 取り込んだもの

| | 機能 | 実測 | コミット |
|---|---|---|---|
| ① | **スナイパーを貫通役に** — 貫通3／密集優先（`dense`）／貫通軌跡 | 500体の群れに1200フレーム撃って**軌跡483本すべてが複数貫通、最大4体** | `fa13261` |
| ② | **配置UI** — 武器選択→暗転して壁が光る→タップ→向き・射界をバー→「配置を変える」で移動 | 4項目を検証。**移動しても向きと射界が残る** | `8dce18e` |
| ③ | **撃破・コイン獲得演出** — 倒した位置からHUDへ◈が飛ぶ | 1200フレームで延べ900・同時最大4（上限26） | `be23921` |
| ④ | **広大マップを独立した実験ステージとして追加**＋カメラ | 1タイル36.6pxを維持したまま30×42を切り取る | `511bc7f` |
| （前） | **敵同士の押し合い**（`crowdOn` で旧挙動に戻せる） | 重なり 96%→4%（400体）／97%→5%（900体） | `cfa4914` |

### 未取り込み

| 機能 | 理由 |
|---|---|
| **凍結（支援武装）** | ご指示どおり**試作のまま独立**させています。既存武器の役割は変えていません |
| 既存5マップの改修 | ご指示どおり**1文字も変えていません** |
| バランス調整 | ご指示どおり**現在の値を維持**（5節） |

---

## 2. §1・§2 のご指示への回答

### §1 「既存マップのまま、壁に置く／通れる地面と分ける仕様が成立するか」

**成立していました。** 既存マップは元から `#`（配置可能な壁・地面）と `.`（敵が通る）を文字で分けており、`buildable()` は `#` のみを返します。**新しく作る必要はありませんでした。**

配置UIの取り込み②で、その区別が**画面でも見える**ようになりました（暗転＋置ける場所が光る）。

### §1 「開けた地形に島が点在する構造を1マップだけ新規作成」

**2枚作りました**（`ex1` は標準サイズ、`ex2` は広大）。`tools/genexp.py` で生成し、到達性と経路長を検証済みです。

| | サイズ | 通れる | 置ける壁 | 通れる割合 | 経路長 |
|---|---|---|---|---|---|
| **ex1 実験場** | 15×21 | 190 | 125 | **60%** | 24, 23, 24 |
| **ex2 実験場・広大** | 30×42 | 808 | 452 | **64%** | 54, 53, 48 |
| （参考）既存 st1 | 15×21 | 92 | 209 | 29% | 45 |

### 既存の進行から完全に切り離しました

実験ステージを足すと突破数の分母が7になり、スキル解放・転生・ミッションが狂います。`MAIN_STAGES`（実験用を除いたもの）を作り、**進行はすべてそちらだけで数える**ようにしました。

- **突破数の分母は 5 のまま**（実験をクリアしても増えない）
- **実験はいつでも遊べる**。本編の鎖（前を突破すると次が開く）には入れない
- **報酬もパックも出ない**。ここで稼げると本編の経済がぶれる
- 結果画面も「実験用なので報酬は出ません」と正しく出す

> **見つけた不具合:** 直す前は、実験ステージの結果画面が「基本パック ×2 が手に入る」と**嘘を表示**していました。修正済みです。

### §2 カメラ

**縮小して全部映すのではなく、等倍のまま一部を切り取ります。** 全部映すと1タイルが小さくなりすぎて、密集も配置も見えなくなるためです。

- **1タイルの見える大きさは 36.6px で、既存ステージとまったく同じ**
- なぞると見る場所が動く（**9px以上動いたらタップではなく移動**と見なすので、配置と干渉しません）
- 右上に今どこを見ているかの小窓。**画面に収まるステージでは出しません**

---

## 3. スマホ実機で確認したこと / 未確認のこと

### 確認したこと（ブラウザの375×812エミュレーション）

- 配置UIの4つの流れ（置く・バーで調整・移動・通路は拒否）
- 広大ステージで1タイルが既存と同じ大きさに見えること
- なぞると見る場所が動くこと
- カメラ操作と配置が干渉しないこと（移動量9pxのしきい値）

### **未確認**（ご自身の実機でお願いしたいこと）

- **実機の fps。** ブラウザ枠は `requestAnimationFrame` を絞るので、ここでは測れません
- 指でのなぞり心地（しきい値9pxが適切か）
- バーのつまみが指で掴みやすいか
- 暗転の濃さが見やすいか

---

## 4. 敵数・描画負荷・敵の密集状態

| 敵数 | 押し合いなし | 押し合いあり |
|---|---|---|
| 400体 | 0.09ms／重なり **96%** | 0.31ms／重なり **4%** |
| 900体 | 0.18ms／重なり **97%** | 1.15ms／重なり **5%** |

- 900体を15秒走らせて「**一度も前進しなかった敵は0体**」
- 本体で600体を描画し、**通路いっぱいに詰まった帯**になることを確認

**これはすべてPCでの数値です。実機の描画負荷は未測定です。**

---

## 5. 武器配置とカメラ操作の問題

> ### 【訂正】「5基では守れない」は問題ではありませんでした
>
> 私は**ガトリング3＋スナイパー2＝5基で守る前提**で測り、「仕様の衝突」として代替案A/B/Cを出しました。
> **この枠組み自体が誤りです。**
>
> 本作は **武器4種を同時に編成し、スキルツリーで設置数を伸ばし、後半のインフレで攻略する**設計です。
> コールドスタートの5基で広大ステージを守れないのは、**不具合ではなく正常な進行**でした。
> A/B/Cの問いは取り下げます。

### 実装を数え直しました

| 増設基盤 | 多く置ける4種 | 合計 | 控えめな4種 |
|---|---|---|---|
| Lv0（開始時） | gatling×3 katana×3 shuriken×3 sniper×2 | **11基** | 4基 |
| Lv2 | ×5 ×5 ×5 ×4 | **19基** | 12基 |
| **Lv5（最大）** | ×8 ×8 ×8 ×7 | **31基** | 24基 |

同時に選べる武器は4種（`loadoutSlots: 4`）、初期設置数は武器ごとに1〜3基、増設基盤（最大Lv5）が全武器を+1ずつ伸ばします。

**広大ステージ（ex2）の置ける壁は452マスあるので、終盤の31基は余裕で収まります。** 私が測った「5基で272体に抜けられた」は、**スキルを1つも買っていない状態の数字**でした。

（なお、**設置数を増やすのは増設基盤だけ**です。カテゴリ別ノードが伸ばすのは火力・射程・レート・会心・範囲・持続で、設置数ではありません。）

### カメラ操作の問題

今のところ見つかっていません。9pxのしきい値で配置と干渉せず、小窓も収まるステージでは出ません。**ただし指での感触は未確認です。**

---

## 6. 旧バージョンと比較して変化した挙動

| | 旧 | 新 |
|---|---|---|
| 敵の重なり | ペアの96〜97%が重なる | **4〜5%** |
| スナイパー | 最も硬い敵（後方のタンク）を狙い、貫通1 | **敵が濃いほう**を狙い、**貫通3**＋軌跡 |
| 配置 | 地面をタップ（どこに置けるか不明）／なぞって向ける | **選ぶ→暗転して光る→タップ→バー** |
| 移動 | 撤去→置き直し（向きと射界が消える） | **「配置を変える」で運ぶ。向きと射界は残る** |
| 撃破 | boom 1種のみ。コイン獲得は数字が増えるだけ | **コインが飛ぶ** |
| ステージ | 5枚 | 5枚＋**実験用2枚**（進行には影響しない） |

### バランスは変えていません（§5のご指示どおり）

押し合いと貫通で難易度が動いたかを比較しました。

| | 押し合いなし | 押し合いあり |
|---|---|---|
| 到達 | 5/5 | 5/5 |
| 1周 | 27.8分 | 26.7分 |
| 待ち率 | 62% | 58% |

**差はすべて、以前測った同条件のばらつき（1周25.8〜26.4分／待ち率61〜63%／試行15〜20）の範囲内です。** 敵HP・ウェーブ数・報酬は1つも変えていません。

**`crowdOn` による旧挙動への切り替えは維持しています。**

> **バランス調整は、現時点では不要です。** 武器4種＋スキルツリーでの設置数拡張＋後半のインフレで攻略していく設計なので、
> 「今この瞬間に守り切れるか」を基準に数字を動かす工程そのものが要りません。

---

## 7. 次に実装する機能と、その完了条件

**⑤（実機プレイ）のあとに決めたいので、今は着手していません。** 候補は以下です。

| 候補 | 完了条件 |
|---|---|
| 凍結を本体へ（試作武器として独立） | 既存武器の役割を変えずに、凍結の効果・持続・詰まり方・撃破効率を観察できる |
| 既存5マップの改修 | **⑤の判断待ち。** 非可逆なので勝手には進めません |

---

## 8. 実プレイで判断していただきたいこと

| | 見ていただきたいところ |
|---|---|
| **1** | **既存マップ（st1など）と実験場（ex1）を続けて遊んで、どちらが本作の方向性か。** 通れる割合が29% 対 60%で、敵の見え方がまったく違います |
| **2** | **実験場・広大（ex2）のカメラ操作。** なぞる感触、小窓の分かりやすさ、配置と干渉しないか |
| **3** | **配置UIの手触り。** 「選ぶ→光る→タップ→バー」で迷わないか。バーのつまみは掴みやすいか |
| **4** | **スナイパーの貫通。** 群れに刺さる感じが出ているか |
| **5** | **コインが飛ぶ演出。** 倒して儲かる実感につながっているか／邪魔ではないか |
| **6** | **実機の fps。** 特に敵が多いとき |


---

## 付録：変更したファイル

| ファイル | 変更 |
|---|---|
| `src/combat.js` | 敵の押し合い（Crowd）、貫通の軌跡、コイン演出 |
| `src/weapons.js` | スナイパーの `pierce` と `target` |
| `src/render.js` | カメラ、小窓、暗転＋強調、軌跡とコインの描画 |
| `src/ui.js` | 向き・射界のバー、「配置を変える」、実験ステージの案内 |
| `src/main.js` | タップ配置、なぞって移動 |
| `src/state.js` | `moveUnit`、実験ステージを進行から除外 |
| `src/stages.js` | `ex1` / `ex2` を追加、`MAIN_STAGES` |
| `src/balance.js` | `crowdOn` など押し合いのノブ |
| `src/packs.js` `src/skilltree.js` | 進行の数え方を `MAIN_STAGES` に |
| `tools/genexp.py` | 実験ステージの生成（新規） |

**既存5マップの定義は1文字も変えていません。**
